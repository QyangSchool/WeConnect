<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.WeConnect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/primary</item>
    </style>

    <style name="Theme.WeConnect" parent="Base.Theme.WeConnect" />
    <style name="dialogue">
        <item name="android:windowBackground">@android:drawable/dialog_holo_light_frame</item>
        <item name="windowNoTitle">true</item>
        <item name="windowMinWidthMajor">70%</item>
        <item name="windowActionBar">false</item>
    </style>
</resources>